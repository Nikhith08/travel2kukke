import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import { VitePWA } from 'vite-plugin-pwa'

// https://vite.dev/config/
export default defineConfig({
  plugins: [
    react(),
    VitePWA({
      registerType: 'autoUpdate',
      includeAssets: ['favicon.ico', 'apple-touch-icon.png', 'mask-controlled.svg'], // These are currently placeholders/to-be-added
      // includeAssets: ['vite.svg'], // Simplified for initial build

      manifest: {
        name: 'Travel2Kukke',
        short_name: 'TravelKukke',
        description: 'Explore Hotels, Cabs, and Places in Kukke Subramanya',
        theme_color: '#2e7d32',
        icons: [
          {
            src: 'pwa-192x192.png',
            sizes: '192x192',
            type: 'image/png'
          },
          {
            src: 'pwa-512x512.png',
            sizes: '512x512',
            type: 'image/png'
          },
          {
            src: 'pwa-512x512.png',
            sizes: '512x512',
            type: 'image/png',
            purpose: 'any maskable'
          }
        ]
      }
    })
  ],
})
